spring:
  datasource:
    url: jdbc:h2:mem:testdb
    #  to store locally url: jdbc:h2:file:/data/demo
    username: sa
    password:
    driver-class-name: org.h2.Driver

  h2:
    console:
      enabled: true
      path: /h2-console

